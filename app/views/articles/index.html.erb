<h1> Aritcles listing page </h1>

<table>
    <thead>
        <tr>
            <th>Title</th>
            <th>Description</th>
            <th>Actions</th>
        </tr>
    </thead>

    <tbody>
        <% @articles.each do |article| %>
            <tr>
                <td> <%= article.title %> </td>
                <td> <%= article.description %> </td>
                <td> <%= link_to "Show", article_path(article), method: :show %> <%= link_to "Edit", edit_article_path(article), method: :edit %> <%= link_to "Delete", article_path(article), data: { turbo_method: "delete"} %> </td>
            </tr>
        <% end %>
    </tbody>
</table>
<br><br>
<%= link_to "Create New", new_article_path%>
<br><br>
<%= link_to "Home page", root_path %>
<br><br>
<%= link_to "About page", about_path %>